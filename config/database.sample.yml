default: &default
  adapter: postgresql
  host: localhost
  encoding: unicode
  pool: 5

development:
  <<: *default
  database: cursos_ufpb_development
  username:
  password:

test:
  <<: *default
  database: cursos_ufpb_test
  username:
  password:
